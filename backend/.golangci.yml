linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gocyclo
    - unused
    - gofmt
    - goimports

linters-settings:
  gocyclo:
    min-complexity: 10

run:
  timeout: 5m
  modules-download-mode: readonly
